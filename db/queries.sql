let view_department=`SELECT department AS department_id FROM department`
let view_role=`SELECT role AS role.id FROM department`
let view_emps=`SELECT employee AS employee from department`
let add_dept=`INSERT INTO department (name)`
let add_emp=`INSERT INTO roles`
let update_emp=`UPDATE employee SET role.id`
